{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capabilities granted to the main window. Keep narrow; widen only when needed.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
